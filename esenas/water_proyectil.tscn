[gd_scene load_steps=6 format=3 uid="uid://bny5wam4hxu67"]

[ext_resource type="Script" uid="uid://c46vvu88rk0af" path="res://scripts/water_proyectil.gd" id="1_sf2w6"]

[sub_resource type="SphereShape3D" id="SphereShape3D_aemat"]
radius = 0.353677

[sub_resource type="Shader" id="Shader_sf2w6"]
code = "shader_type spatial;
render_mode blend_mix, cull_back, unshaded, depth_prepass_alpha;

// Colores
uniform vec4 bubble_color : source_color = vec4(0.6, 0.85, 1.0, 0.25);
uniform vec3 rim_color    : source_color = vec3(1.0, 1.0, 1.0);

// Toon lighting
uniform vec3 light_color  : source_color = vec3(1.0, 1.0, 1.0);
uniform vec3 shadow_color : source_color = vec3(0.5, 0.6, 0.8);
uniform float toon_threshold : hint_range(0.0, 1.0) = 0.5;

// Fresnel rim
uniform float rim_intensity : hint_range(0.0, 3.0) = 1.0;
uniform float rim_power : hint_range(0.5, 8.0) = 2.0;

uniform float wobble_speed : hint_range(0.0, 10.0) = 0.0;
uniform float wobble_amp   : hint_range(0.0, 0.1) = 0.02;

void vertex() {
    if (wobble_speed > 0.0 && wobble_amp > 0.0) {
        float w = sin(TIME * wobble_speed + VERTEX.x * 3.1 + VERTEX.y * 2.7 + VERTEX.z * 1.9) * wobble_amp;
        VERTEX += NORMAL * w;
    }
}

void fragment() {
    // Dirección de la luz principal (Godot 4 te da LIGHT y LIGHT_COLOR)
    vec3 light_dir = normalize(light_color);
    float ndotl = max(dot(NORMAL, light_dir), 0.0);

    // Toon shading
    vec3 toon_col = ndotl > toon_threshold ? light_color : shadow_color;

    // Fresnel
    vec3 view_n = normalize(NORMAL);
    vec3 view_dir = normalize(-VIEW);
    float fres = pow(1.0 - clamp(dot(view_n, view_dir), 0.0, 1.0), rim_power);

    // Resultado final
    ALBEDO = mix(bubble_color.rgb * toon_col, rim_color, fres * rim_intensity);
    ALPHA  = bubble_color.a + fres * 0.3;
}
"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_vveip"]
render_priority = 0
shader = SubResource("Shader_sf2w6")
shader_parameter/bubble_color = Color(0.266667, 0.435294, 0.878431, 0.917647)
shader_parameter/rim_color = Color(0.271685, 0.395036, 0.742684, 1)
shader_parameter/light_color = Color(1, 1, 1, 1)
shader_parameter/shadow_color = Color(0.347855, 0.617491, 0.895255, 1)
shader_parameter/toon_threshold = 0.92
shader_parameter/rim_intensity = 0.685
shader_parameter/rim_power = 2.673
shader_parameter/wobble_speed = 7.291
shader_parameter/wobble_amp = 0.034

[sub_resource type="SphereMesh" id="SphereMesh_yuhjx"]
material = SubResource("ShaderMaterial_vveip")

[node name="Water_proyectil" type="RigidBody3D"]
collision_layer = 4
collision_mask = 25
mass = 0.5
script = ExtResource("1_sf2w6")
gravedad = 9

[node name="CollisionShape3D" type="CollisionShape3D" parent="."]
shape = SubResource("SphereShape3D_aemat")

[node name="MeshInstance3D" type="MeshInstance3D" parent="."]
layers = 2
mesh = SubResource("SphereMesh_yuhjx")
skeleton = NodePath("../CollisionShape3D")

[connection signal="body_entered" from="." to="." method="_on_body_entered"]
